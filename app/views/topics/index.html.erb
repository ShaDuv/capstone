
<div class="banner">
</div>
<div class="container">
  <h4> This is some sample text to tell the user what they need to enter here and what they are expecting to see when they get their results"

  <div class="row searcbox">
    <form class="col s12">
      <div class="row">
        <div class="input-field col s8">
          <div><%= form_tag(topics_path, method: :post, class: "") do %>
          <%= text_field_tag :term, params[:term], class:"" %>
          <%= submit_tag 'Search', class: "waves-effect waves-light btn"  %>
          <% end %>
          </div>
        </div>
      </div>
    </form>
  </div>

</div>
