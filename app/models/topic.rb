class Topic < ApplicationRecord
  has_many :profiles

#   def find_by_name()
#   # will need to add a function here that will search the database to see if the term is already there and if so pull from that if not then pull from scraper.
#   # end
#
end
